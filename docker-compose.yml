services:
  core:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config:/config
      - ${VAULT_PATH:-./vaults/default}:/vaults/default
    environment:
      ARCHIVEOS_CONFIG: /config
      ARCHIVEOS_LISTEN: 0.0.0.0:8080
