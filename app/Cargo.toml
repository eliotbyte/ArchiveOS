[package]
name = "archive-os"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "archive-os"
path = "src/main.rs"

[dependencies]
archiveos-core = { path = "../core" }
archiveos-contract = { path = "../contract" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde_json = "1"
